export function estadoCieloToEmoji(estado) {
    if(estado === "soleado"){
    return estado = "☀️"}
     else if (estado === "nublado" ){
    return estado = "☁️"
} else if ( estado === "lluvioso"){
    return estado = "🌧️"
}
}